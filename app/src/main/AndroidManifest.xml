<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fluc.siservis_comanda">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_siservis"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_siservis_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MesasActivity" />
        <activity android:name=".TipoProductoActivity" />
        <activity android:name=".ProductoActivity" />
        <activity android:name=".ComandaActivity" />
        <activity android:name=".EspecificacionesActivity" />
        <activity android:name=".AgregarCancelarActivity" />
        <activity android:name=".DescuentoActivity" />
        <activity android:name=".ComandaMotivoCancelacionActivity" />
        <activity android:name=".LoginParametrosActivity"></activity>
    </application>

</manifest>